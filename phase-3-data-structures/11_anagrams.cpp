#include<iostream>
#include <vector>
using namespace std;

bool isAnagram(string a, string b)
{
    int length1, length2;
    length1 = a.length();
    length2 = b.length();
}

int main()
{
    vector<string> words;
    vector<string> anagrams;

}